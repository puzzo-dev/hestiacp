# Files shipped by the hestia-ivarse package, relative to the repository root.
#
# Every path here lands under /usr/local/hestia/ and MUST NOT be a path the
# upstream hestia package owns. Adding an upstream-owned path here will make
# dpkg refuse the install, which is the intended safeguard: this package is an
# addition to Hestia, never a replacement for part of it.
#
# Check a candidate path with:
#   dpkg -S /usr/local/hestia/<path>
#
# Directories are shipped recursively. Blank lines and # comments are ignored.

# Node.js runtime management
bin/v-add-sys-nodejs
bin/v-delete-sys-nodejs
bin/v-list-sys-nodejs
bin/v-change-sys-nodejs-default
func/node.sh

# Node.js release signing keys, used to verify SHASUMS256.txt
install/common/nodejs

# Application registry
func/app.sh
bin/v-list-node-app
bin/v-list-node-apps

# Node.js release key refresh
bin/v-update-sys-nodejs-keys

# Application lifecycle
bin/v-add-node-app
bin/v-delete-node-app
bin/v-change-node-app-port
bin/v-change-node-app-runtime
bin/v-change-node-app-command
